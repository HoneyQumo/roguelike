#pragma once

namespace RoguelikeGame
{
    /**
    *	Превращает падение игры в запись в логе.
    *
    *	Без этого Windows просто закрывает окно: лог обрывается на полуслове,
    *	код возврата -1, и о причине сказать нечего. Ровно так пропал краш на мосту.
    *
    *	Ставит обработчики на все шесть способов умереть - SEH, векторный перехват,
    *	terminate, abort и segfault, неверный параметр CRT и assert в отладке, -
    *	и на любом из них разматывает стек с именами функций и номерами строк.
    *
    *	Имена берутся из .pdb рядом с exe. Без него в логе останутся голые адреса,
    *	поэтому .pdb кладётся в релиз вместе с игрой.
    *
    *	Звать один раз в самом начале main, до всего остального.
    */
    void InstallCrashReport();
}
