#pragma once

namespace RoguelikeGame
{
    /**
    *	Пишет в лог причину и стек, когда игра падает. Без этого Windows просто
    *	закрывает окно, и разбираться не с чем.
    *
    *	Имена функций берутся из .pdb рядом с exe, поэтому он едет в релиз вместе с игрой.
    *	Звать первым делом в main.
    */
    void InstallCrashReport();
}
