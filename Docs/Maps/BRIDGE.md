# Мост

Длинная локация между тюрьмой и городом: 22 секции по 32 клетки, 704×24 всего. Игрок идёт слева направо и отбивается.

Собирает `Docs/Maps/make_bridge.py`. На выходе - обычные `.config` комнат и акт, которые дальше живут в репозитории как есть: их можно править руками или в mapper, скрипт нужен только чтобы разложить первый вариант.

## Полотно

По вертикали у каждой секции одно и то же:

| строки | что |
|---|---|
| 0-4 | вода |
| 5 | отбойник |
| 6-10 | полоса |
| 11 | разделительная разметка |
| 12-16 | полоса |
| 17 | отбойник |
| 18-23 | вода |

Шаблон рисует поверх этой основы и не трогает крайние колонки - иначе не сойдутся швы между секциями. Торцы моста закрыты отбойником: за первой секцией и за последней ехать некуда.

## Шаблоны секций

| шаблон | что даёт |
|---|---|
| чистый пролёт | редкие машины, покрышки, следы шин - место отдышаться |
| пробка | машины в четыре ряда, масляные пятна |
| блокпост | стена барьеров поперёк с проходом по центру |
| авария | сгоревшие машины, лужи масла, следы торможения |
| топливо | две гряды бочек вдоль полос - место для цепного взрыва |
| пандус | карман за отбойником с тремя ящиками: патроны, аптечка, броня |
| обрушение | половина полотна съедена водой, обойти можно вдоль отбойника |

Порядок задан руками, а не арифметикой по номеру секции: обрушения и пандусы должны попадаться регулярно, иначе половина моста выходит пустой. На 22 секции приходится по четыре пандуса и обрушения, четыре пробки, по три аварии и гряды топлива, два блокпоста.

## Сложность

Врагов становится больше к концу: от одного в начале до пяти в последних секциях. Состав тоже меняется - сначала только мародёры, ближе к середине добавляются штурмовики и щитовики, в конце тяжёлые и радисты.

## Как перебрать карту

```
python Docs/Maps/make_bridge.py
```

Скрипт детерминирован: один и тот же seed даёт одну и ту же карту. Менять раскладку - правкой `SHAPES`, плотность - внутри шаблонов.

Результат проверяется тем же валидатором, что и остальные локации: границы закрыты, выход достижим, вода не режет путь. Проверка идёт в `ShippedLevelsTest`, отдельного запуска не требует.

## Погоня

Мост - беговая локация: игрок сбежал из тюрьмы, и за ним идут. Волны тут не годятся - их логика «остановись, убей всех, иди дальше» работает против бега. Вместо них погоня, которая ничего не ждёт и не кончается.

Описана в акте:

```
[pursuit]
keep 4            ; сколько держится на хвосте
grow 9            ; до скольки растёт, если стоять на месте
respawn 2.5       ; через сколько приходит замена убитому

from 0.0  m4 g3   ; кто выбегает на этом отрезке пути
from 0.5  a4 s2 m2
```

`from` - доля пройденного пути, дальше группы «символ врага + вес». Кто именно выбежит, решает жребий по весам: два забега не дают одинаковой погони. Отрезки обязаны идти по возрастанию, иначе выбор читал бы не ту строку - загрузчик такой файл отвергает.

### Что гонит вперёд

Напор растёт, пока игрок топчется на месте: каждые `PURSUIT_GROW_TIME` на хвосте становится на одного больше, до `grow`. Продвинулся на `PURSUIT_RELAX_STEP` - напор падает обратно к `keep`.

Игрок сам регулирует сложность скоростью, и выгодно бежать. Отбиться насовсем нельзя: убитого через `respawn` сменяет следующий.

### Откуда они берутся

Строго из-за спины. Направление компонент не знает как «направо» - он берёт вектор от входа на мост до машины, и это работает на любой карте. Точка должна быть позади игрока вдоль этого вектора и в окне `PURSUIT_SPAWN_GAP`-`PURSUIT_SPAWN_REACH`: ближе видно рождение, дальше не догонит.

Точки - те же клетки `*`, что и у волн. Они отвечают только за «где».

### Машина

Открыта с самого начала. Гейта «отбей всё» нет - преградой служит сама дистанция. Добежал - уехал.

### Заслоны

Статические враги в секциях просыпаются при входе (`RoomWakeComponent`) и работают заслонами: сзади догоняют, впереди не пускают. Это отдельная от погони механика, она живёт на карте.

**Караул смотрит навстречу.** На локации с `[pursuit]` все расставленные враги разворачиваются лицом к тому, откуда придёт игрок - угол берётся из направления «вход → выход». На обычной карте угол не трогаем: там часовой сам решает, куда глядеть, и разворот сломал бы расчёт зон видимости, на котором держится скрытность в тюрьме.

**Блокпост охраняется.** Шаблон `Checkpoint` ставит не только барьеры: щитовик держит проход, стрелок стоит за барьером, и через один пост к ним добавляется караульный с маршрутом вдоль заслона. Проход в барьере оставлен намеренно - заслон должен тормозить бегущего, а не запирать мост.

Двое стрелков с фланга вместе с погоней за спиной оказались перебором, поэтому остался один. Караул выходит **через раз**, а не по жребию: блокпостов на мосту всего два, и монетка легко оставляла без патруля оба сразу.

Гарнизон идёт в общий счёт врагов секции. Иначе случайная расстановка доставляла людей поверх заслона, и ослабление поста ничего не меняло.

Маршрут караула именуется по секции (`block08`), иначе точки из разных секций слиплись бы в один обход длиной во весь мост.

### Волны

`WaveDirectorComponent` никуда не делся и пригодится городу, где будет настоящая арена: заперли на складе, отбей четыре волны, дверь открылась. Там его модель на месте. Уровень может нести и то, и другое - секции `[waves]` и `[pursuit]` независимы.

### Проломы

Шаблон `Collapse` вырезает кусок полотна до воды и обводит его край в накладке символом `Breach`.

Форма пролома нарочно неровная. Глубина гуляет вокруг середины **редкими крупными уступами**, а не меняется каждую колонку: от частых мелких зубцов край читается как корона, а не как обвалившаяся плита. К бокам пролом мельчает - так он выглядит провалившимся куском, а не вырезанным прямоугольником.

Какой именно кусок бетона рисовать, решает движок по маске соседей. В генераторе карты достаточно поставить `Breach` на каждую клетку воды, у которой есть сосед-дорога.

Проход вдоль отбойника остаётся всегда: пролом обязан тормозить и пугать, а не запирать мост. Это проверяется тестом - в каждой колонке мостового полотна должен остаться проходимый ряд.

Провалиться в воду нельзя: по краю воды движок и так ставит невидимую стену. Накладка проходимость не меняет вовсе.

Рядом с проломом раскидано четыре-семь кусков плиты (`bridge_rubble`) - обвал не бывает аккуратным.

## Отбойник и обломки

Стены моста - не обычная кладка: генератор тайлсетов рисует для него кирпичный парапет с металлической балкой поверху. Балка появляется только там, где парапет тянется вдоль - у клетки есть сосед слева или справа. На торцах остаётся голая кладка, иначе отбойник обрывался бы в воздухе.

По краям обрушенных пролётов лежат обломки: куски бетона с торчащей арматурой и крошка вокруг. Это два пропа, `bridge_rubble` и `bridge_rubble_wide`, оба непроходимыми не считаются - обойти дыру по узкой полосе они не мешают.

## Повороты

У пропа два источника угла.

**Из карты** - через `@` в легенде: `C Prop:car_sedan@72` ставит машину поперёк полосы. Так собраны сцены ДТП, где машины сбились в кучу.

**Из каталога** - поле `jitter <градусы>`: небольшой разброс, чтобы брошенные машины не стояли по линейке. Угол берётся из хеша клетки, поэтому одна и та же позиция всегда даёт один и тот же наклон - карта не «дрожит» между запусками.

Оба складываются: машина, поставленная под 72°, всё равно получит свой небольшой доворот.
